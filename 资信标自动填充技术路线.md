
资信标自动填充系统 -- 完整技术路线文档

## 文档定位
- **目标读者**: 后端开发工程师、产品经理
- **核心目标**: 实现投标文件组成模板的自动填充,内容准确且原格式100%保留
- **技术架构**: 解析 -> AI辅助打标 -> 标签提取 -> 企业信息匹配 -> 自动化填充

---

## 一、核心概念科普(产品经理必看)

### 1.1 Word文档的底层结构(俄罗斯套娃)

Word文档(.docx)在底层是一个复杂的XML结构。`python-docx` 库将其封装为以下层级:

```
Document (文档)
  ├── Paragraphs (段落) -- 正文
  │     └── Runs (文字块) <- 最关键!
  └── Tables (表格)
        └── Rows (行)
              └── Cells (单元格)
                    └── Paragraphs (段落)
                          └── Runs (文字块)
```

### 1.2 什么是 Run?(后端开发必看)

**Run(文字块)** 是 `python-docx` 中最核心的概念。

- **定义**: 一个段落中,如果一段文字的格式(字体、字号、颜色、加粗、下划线)完全一致,它就是一个 Run。
- **碎片化现象**: 如果一句话中间某个词被加粗了,这一行就会被拆成多个 Run。例如:
  - 原文: "**投标人**:________"
  - 底层 Run 分布:
    - Run 1: "投标人"(加粗)
    - Run 2: ":"(普通格式)
    - Run 3: "________"(下划线格式)

**关键原则**: 替换时必须操作 Run,不能直接修改段落文本,否则会丢失所有格式。

### 1.3 为什么不能直接让大模型输出完整文档?

| 方案 | 问题 | 结论 |
|------|------|------|
| 给大模型纯文本 | 大模型返回纯文本后,Python无法将其塞回Word XML,格式全丢 | 不可行 |
| 给大模型Word XML源码 | Token爆炸,大模型写错一个闭合标签(如 `</w:r>`),文档直接损坏打不开 | 不可行 |
| **打标签方案**(本文采用) | 大模型只处理纯文本,Python负责格式,各司其职 | 黄金架构 |

---

## 二、五步技术实现路线

### 第一步: 文档预处理(PDF转Word + 加载)

**业务逻辑**: 用户上传的文件可能是 Word 或 PDF,统一转换为 Word 格式。

**技术实现**:

```python
# 1. PDF 转 Word(使用 pdf2docx 库)
from pdf2docx import Converter

def pdf_to_word(pdf_path, word_path):
    cv = Converter(pdf_path)
    cv.convert(word_path, start=0, end=None)
    cv.close()

# 2. 加载 Word 文档
from docx import Document

doc = Document('投标文件模板.docx')
```

**⚠️ 风险预警**:
- PDF 转 Word 后会产生大量碎片化 Run(一个词可能被拆成多个 Run),后续处理时必须先合并格式相同的相邻 Run。

---

### 第二步: AI辅助打标(核心环节)

**业务逻辑**: 让大模型识别文档中所有需要填写的地方,并打上 `{{标签}}` 占位符；

**技术实现**:

#### 2.1 后端提取纯文本(大模型看不到XML)

```python
# 遍历正文段落
for para in doc.paragraphs:
    text = para.text  # python-docx 自动过滤XML,只返回纯文本
    # 将 text 发送给大模型打标

# 遍历表格(俄罗斯套娃: 文档 -> 表格 -> 行 -> 单元格 -> 段落)
for table in doc.tables:
    for row in table.rows:
        for cell in row.cells:
            for para in cell.paragraphs:
                text = para.text
                # 将 text 发送给大模型打标
```

**文本打标**

#### 2.2 普通文本段落 AI 索引式语义分块方案

##### 2.2.1 方案设计核心思想

- 不依赖硬编码正则识别标题（规避PDF转Word、非标文档格式混乱识别失效问题）；
- 后端给每个有效段落**绑定物理数字索引**，发给大模型仅输出语义块起始索引数组，无文本匹配歧义；
- 后端根据索引精准物理切割语义块，**整块送入模型打标**，自带完整章节上下文，解决「地址/姓名」等泛标签问题；
- 增加**强鲁棒性兜底机制**：AI分块异常、输出非法格式、索引越界、语义切割失效时，自动降级为**固定20段落机械分块**，不中断业务流程。

##### 2.2.2 阶段1：后端生成带物理索引的段落文本

过滤空段落，为每段有效文本绑定唯一序号，用于大模型语义边界识别。

```python
def get_valid_numbered_paragraphs(doc):
    """
    提取文档有效文本段落，绑定物理索引
    返回：带索引文本、原始段落对象列表
    """
    valid_paragraphs = []
    for para in doc.paragraphs:
        txt = para.text.strip()
        if txt:
            valid_paragraphs.append(para)
    
    # 拼装带索引的纯文本
    numbered_lines = [f"[{idx}] {p.text.strip()}" for idx, p in enumerate(valid_paragraphs)]
    numbered_text = "\n".join(numbered_lines)
    return numbered_text, valid_paragraphs
```

##### 2.2.3 阶段2：语义分块专用Prompt（仅输出索引数组）
```
【角色】文档结构分析专家
【任务】
我将提供一份标书文档纯文本，每一行开头有中括号`[数字]`代表段落物理序号。
请根据语义识别所有全新业务模块、核心章节的**起始段落序号**。

【判断依据】
1. 标准章节标题：一、xxx、1.1 xxx、第x章等；
2. 无标准标号但独立成行、开启全新业务主题的段落；
3. 忽略普通正文换行、无关短句。

【输出要求】
1. 仅输出语义块起始段落序号，以纯JSON一维数组返回；
2. 禁止输出解释、多余文字、代码块；
3. 严格只返回数字数组，示例：[0,15,34]
```

##### 2.2.4 阶段3：后端索引解析 + 鲁棒性兜底校验
清洗大模型输出、校验索引合法性，异常自动降级固定20段落分片。
```python
import re
import json

def get_block_indices_with_fallback(llm_response_text, total_paragraphs, chunk_size=20):
    """
    解析LLM语义分块索引，失败自动兜底为固定段落分片
    """
    try:
        # 清洗提取JSON数组
        match = re.search(r'\[[\d,\s]*\]', llm_response_text)
        if not match:
            raise ValueError("未识别到索引数组")
        
        indices = json.loads(match.group(0))
        # 类型校验
        if not isinstance(indices, list) or not all(isinstance(x, int) for x in indices):
            raise ValueError("索引包含非整数")
        
        # 过滤越界索引、去重、补首尾边界
        valid_indices = [idx for idx in indices if 0 <= idx < total_paragraphs]
        if 0 not in valid_indices:
            valid_indices.insert(0, 0)
        valid_indices.append(total_paragraphs)
        final_indices = sorted(list(set(valid_indices)))
        
        # 校验是否有效分块，无切割则触发兜底
        if len(final_indices) <= 2 and total_paragraphs > chunk_size:
            raise ValueError("AI未完成有效语义分块")
        
        return final_indices
    except Exception as e:
        # 兜底：固定20段落机械分块
        fallback = list(range(0, total_paragraphs, chunk_size))
        if fallback[-1] != total_paragraphs:
            fallback.append(total_paragraphs)
        return fallback
```

##### 2.2.5 阶段4：后端物理切割语义块 + 整块打标

按索引切割完整语义块，**整块文本直接送入打标模型**，切割出语义块，块内每一行强制保留 [物理索引] 前缀，原样拼接送给打标模型：

示例输入给模型：
```
[15] 单位名称：________________
[16] 单位地址：________________
[17] 法定代表人：    年  月  日
```

##### 2.2.6 普通文本打标指令

```
【角色】你是一名专业的投标助理,精通各类投标文件模板的语义理解。
【任务】识别文本中所有需要投标人填写、补充或提供的空白处。
【空白处类型包括但不限于】:
1. 下划线:________
2. 括号提示:(项目名称)、[请填写]
3. 散落空格:  年  月  日
4. 冒号留白:投标人:
5. 表格中的空单元格

【打标规则】:
1. 根据上下文语义,为每个空白处生成一个简洁的中文标签
2. 标签格式统一为 {{标签名}}
3. 必须返回原句,只替换空白处为标签,严禁删减或改动原句中的其他任何字符,包括标点符号
4. 绝对禁止生成如 {{地址}}、{{姓名}} 这样指代不明的标签！结合整块文档上下文，生成高度具体的标签
5. 如果空白处原有下划线,请用下划线包裹标签:_{{标签}}_
6. 输入文本每一行开头都带有固定段落索引格式 [数字]，你必须完整保留每一行的 [数字] 索引，不得删除、修改、打乱顺序、合并多行。

【示例】
输入：
[15] 单位名称：________________
[16] 单位地址：________________
[17] 法定代表人：    年  月  日

输出：
[15] 单位名称：{{投标人单位名称}}
[16] 单位地址：{{投标人单位地址}}
[17] 法定代表人：{{年}}年{{月}}月{{日}}日


【待打标文本块】
{{带索引的语义块全文}}
```

##### 2.2.7 后端解析及逐段跨run处理

后端解析规则：
1. 按行遍历模型返回文本，正则匹配 `[数字]` 行首标识；
2. 构建「段落索引 → 打标后段落文本」映射字典；
3. 根据索引下标，精准匹配内存中原始 `valid_paragraphs` 段落对象；
4. **逐段落调用跨Run替换函数**，完成标签回填、格式保留。

解析工具函数：
```python
import re

def parse_tagged_block_to_lines(tagged_block_text):
    """
    解析模型返回的带索引文本块
    返回：dict {索引: 打标后纯段落文本}
    """
    line_map = {}
    pattern = r'^\[(\d+)\]\s*(.*)$'
    for line in tagged_block_text.splitlines():
        line = line.strip()
        if not line:
            continue
        m = re.match(pattern, line)
        if m:
            idx = int(m.group(1))
            content = m.group(2)
            line_map[idx] = content
    return line_map
```




**表格打标**

#### 2.3 表格统一预处理

##### 2.3.1 核心设计思想
后端负责统一解析表格及序列化**所有表格无差别使用同一套预处理逻辑**：

##### 2.3.2 表格序列化实现规则

1. **唯一单元格去重提取**

`python-docx` 中跨行跨列合并单元格，会在同行 `cells` 列表重复出现；后端按从左到右顺序，提取**内存地址唯一、不重复**的真实单元格。
空单元格统一替换为占位符 `[空]`。

2. **自定义分隔符结构化拼接**

采用 **`|` 分隔符伪Markdown序列化文本**（非标准化 Markdown，因其不兼容单元格合并等规则）：
- 按行用 `|` 拼接单元格文本；
- 不强制行列对齐、不添加Markdown分隔线；
- 依靠大模型语义理解，自动兼容合并单元格、不规则表格。

**序列化示例**
```
姓名 | [空] | 性别 | [空] | 年龄 | [空]
职务 | [空] | 职称 | [空] | 学历 | [空]
参加工作时间 | [空] | 从事项目负责人年限 | [空]
```

##### 2.3.3 表格上下文增强处理（解决表格孤岛）

**核心痛点**：单独发送表格会丢失表名、章节标题等归属信息，导致AI打标语义模糊；标书中表名可能距离表格多段文字，无法仅抓取上一段。
**最终方案**：**顺序遍历文档 + 上方N段滑动窗口 + 仅保留上文 + 纯表格输出**，彻底解决表格孤岛问题。

---

###### ① 文档顺序遍历函数

保留原有 doc.paragraphs / doc.tables 主体遍历逻辑； 仅在表格上下文增强处理环节，使用 iter_block_items 按物理顺序读取文档，精准匹配表格上方标题信息

```python
from docx.document import Document as _Document
from docx.oxml.text.paragraph import CT_P
from docx.oxml.table import CT_Tbl
from docx.table import Table
from docx.text.paragraph import Paragraph

def iter_block_items(parent):
    """
    按文档物理顺序，逐块返回 Paragraph 和 Table 对象
    解决段落与表格顺序丢失问题
    """
    if isinstance(parent, _Document):
        parent_elm = parent.element.body
    elif isinstance(parent, _Cell):
        parent_elm = parent._tc
    else:
        raise ValueError("不支持的文档对象")

    for child in parent_elm.iterchildren():
        if isinstance(child, CT_P):
            yield Paragraph(child, parent)
        elif isinstance(child, CT_Tbl):
            yield Table(child, parent)
```

###### ② 上方5段滑动窗口（仅上文）

使用双端队列维护**最近5个非空上文段落**，处理完表格立即清空窗口，避免连续表格上下文污染：

```python
from collections import deque
from docx.text.paragraph import Paragraph
from docx.table import Table

def get_tables_with_upper_context(doc, window_size=5):
    """
    功能：遍历文档，为每个表格收集上方N段非空上文
    返回：结构化数据 [ {表格, 上文文本, 序列化表格内容}, ... ]
    """
    upper_context_window = deque(maxlen=window_size)
    # 存储最终结果：每个表格对应独立的上下文信息
    table_context_list = []

    # 按文档真实顺序遍历（仅用于收集上下文，不影响原有逻辑）
    for block in iter_block_items(doc):
        # 处理段落：缓存非空文本作为上文
        if isinstance(block, Paragraph):
            text = block.text.strip()
            if text:
                upper_context_window.append(text)

        # 处理表格：收集上下文，存入列表，然后清空窗口
        elif isinstance(block, Table):
            # 拼接当前表格的所有上文
            table_upper_context = "\n".join(upper_context_window)
            # 表格序列化（你原有的函数）
            serialized_table = serialize_table(block)

            # 结构化存储：纯数据，不做任何拼接
            table_context_list.append({
                "table": block,                  # 原始表格对象（用于后续回填）
                "upper_context": table_upper_context,  # 表格上文（单独输出）
                "serialized_table": serialized_table    # 序列化后的表格
            })

            # 关键：处理完表格，清空上文窗口，避免污染下一个表格
            upper_context_window.clear()

    # 返回所有表格 + 对应上文（纯数据）
    return table_context_list
```


##### 2.3.3 表格打标指令

后端将统一序列化后的表格文本以及表格上文信息发给大模型，由模型自行区分**静态表单**和**动态列表**并按规则打标：

```
【角色】标书模板表格智能打标专家

【任务】接收按|分隔符序列化的表格文本，自主判断表格类型：静态表单 / 动态列表。参考【表格前文背景】，为表格打标，仅处理表格内容。

【判断规则】
1. 静态表单：人员信息、企业资质、固定填报项，特征为属性名+单一项填空；
2. 动态列表：业绩清单、人员名录、设备清单，特征为顶部统一表头、下方多行重复空白行。

【输入】
=== 表格前文背景（仅参考，禁止修改、禁止输出） ===
{upper_context}
=== 待打标表格（|分隔序列化） ===
{serialized_table}

【打标规则】
1. 静态表单：所有[空]逐一替换为独立业务标签{{字段名}}，保留所有行列结构；
2. 动态列表：仅保留表头和第一行空白位，删除后续所有重复空行；
   在首行添加docxtpl循环语法：{{tr for item in 列表名称}}
   单元格使用泛型标签：{{item.字段名}}。
3. 绝对禁止生成如 {{地址}}、{{姓名}} 这样指代不明的标签！你必须结合前文背景（如标题、业务场景），生成高度具体的标签（如 {{投标人单位地址}}、{{项目负责人姓名}}）。

【示例】
输入：
=== 表格前文背景（仅参考，禁止修改、禁止输出） ===
历史业绩表格
=== 待打标表格（|分隔序列化） ===
| 项目名称 | 建设单位 | 合同金额 |
| [空] | [空] | [空] |
| [空] | [空] | [空] |

输出：
| 项目名称 | 建设单位 | 合同金额 |
| {{tr for item in 历史业绩列表}}{{item.项目名称}} | {{item.建设单位}} | {{item.合同金额}} |

【输出要求】
1. 严格保留原有|分隔符，仅替换[空]、增减空行，不改动原有文字和顺序。
2. 只输出打标后的表格文本，禁止输出前文、解释、代码块！
```

---

### 第三步: 标签提取与企业信息匹配

**业务逻辑**: 收集文档中所有 `{{标签}}`,让大模型从企业信息文本中找到对应内容。

**技术实现**:

#### 3.1 标签收集(正则表达式)
```python
import re

# 扫描整篇文档,提取所有唯一的标签名
tags = set()

# 扫描正文
for para in doc.paragraphs:
    matches = re.findall(r'\{\{(.*?)\}\}', para.text)
    tags.update(matches)

# 扫描表格
for table in doc.tables:
    for row in table.rows:
        for cell in row.cells:
            for para in cell.paragraphs:
                matches = re.findall(r'\{\{(.*?)\}\}', para.text)
                tags.update(matches)

print(f"共发现 {len(tags)} 个标签: {tags}")
```

#### 3.2 信息匹配指令(Prompt B 支持普通字段+列表数组)

```
【角色】你是一名专业的企业信息分析师。

【输入】
1. 标签列表:["企业名称", "注册资本", "法定代表人姓名", ...]
2. 企业基础信息文本(长文本)

【任务】请在"企业基础信息文本"中,为"标签列表"里的每一个标签寻找最匹配的具体信息。

【特殊说明：列表类型】
当你看到值为 `[Array]` 的字段时（例如业绩、人员），说明这是一组列表数据。请你在企业信息中寻找所有符合条件的项目，提取完整的列表，输出成 JSON 数组形式（`[{...}, {...}]`）。

【请严格按照以下 JSON 骨架输出，填入真实数据】
{
  "企业名称": "XX科技有限公司",
  "注册资本": "1000万元",
  "法定代表人姓名": "张三",
  "企业网址": "待补全",  // 找不到时返回"待补全"
  "资质证书号": "待补全"
  "历史业绩列表": [
    {
      "建设单位": "待填入",
      "工程名称": "待填入",
      "建设规模": "待填入",
      "质量": "待填入"
    }
  ]
}

【规则】
1. 如果企业信息中找不到该标签对应的内容,Value 请返回"待补全"
2. 不要编造信息,必须基于提供的企业信息文本
3. 金额、日期等数据保持原文格式
4. 直接返回JSON对象，不要输出任何说明或解释性内容
```

---

### 第四步: 内容填充(格式保留的关键)

**业务逻辑**: 将匹配到的企业信息，通过跨Run核心算法精准替换普通段落标签；同时实现**表格文本原路回写、静态表单填空、动态列表docxtpl自动行克隆渲染**，全程100%保留原文档格式、单元格合并样式。

**技术实现**:

#### 4.1 核心算法: 跨 Run 替换(防误删 + 格式保留)

这是整个系统最核心、最难的工程实现点。

**问题背景**:
- 标签 `{{招标人名称}}` 可能跨越多个 Run(如 `{{` 在 Run 1,`招标人名称` 在 Run 2,`}}` 在 Run 3)
- 直接替换 `paragraph.text` 会清空所有格式
- 直接替换单个 Run 可能找不到完整标签

**解决方案: 字符索引映射法 + 首个 Run 保留算法**

```python
def replace_text_in_paragraph(para, old_text, new_text):
    full_text = para.text
    if old_text not in full_text:
        return False

    # 1. 定位目标文本在完整段落中的起始/结束字符位置
    start_idx = full_text.find(old_text)
    end_idx = start_idx + len(old_text)

    # 2. 遍历所有 Run,计算每个 Run 的字符范围,确定哪些 Run 被目标文本跨越
    run_start_idx = 0
    matching_runs = []

    for run in para.runs:
        run_end_idx = run_start_idx + len(run.text)
        if (run_start_idx <= start_idx < run_end_idx or
            run_start_idx < end_idx <= run_end_idx or
            (start_idx <= run_start_idx and run_end_idx <= end_idx)):
            matching_runs.append({
                'run': run,
                'run_start': run_start_idx,
                'run_end': run_end_idx,
                'text_start': max(start_idx, run_start_idx),
                'text_end': min(end_idx, run_end_idx)
            })
        run_start_idx = run_end_idx

    if not matching_runs:
        return False

    # 3. 如果文本只在一个 Run 中,直接替换
    if len(matching_runs) == 1:
        run_info = matching_runs[0]
        run = run_info['run']
        run_local_start = run_info['text_start'] - run_info['run_start']
        run_local_end = run_info['text_end'] - run_info['run_start']
        run.text = run.text[:run_local_start] + new_text + run.text[run_local_end:]
        return True

    # 4. 处理跨多个 Run 的文本替换(核心算法)
    # 策略: 保留第一个 Run 的格式,将替换后的文本放入第一个 Run,清空其他 Run
    first_run_info = matching_runs[0]
    first_run = first_run_info['run']
    first_run_local_start = first_run_info['text_start'] - first_run_info['run_start']

    # 第一个 Run = 目标文本之前的部分 + 新文本
    new_run_text = first_run.text[:first_run_local_start] + new_text
    first_run.text = new_run_text

    # 处理后续的 Run: 删除它们中包含的目标文本部分
    for i, run_info in enumerate(matching_runs):
        if i == 0:  # 第一个 Run 已处理
            continue
        run = run_info['run']
        run_local_start = run_info['text_start'] - run_info['run_start']
        run_local_end = run_info['text_end'] - run_info['run_start']

        if run_local_end < len(run.text):
            # 这个 Run 除了目标文本外还有其他内容,保留后面的内容
            run.text = run.text[run_local_end:]
        else:
            # 整个 Run 都是目标文本的一部分,清空它
            run.text = ""

    return True
```

#### 4.1.1 算法核心执行逻辑

1. **以段落为最小处理单元**：Word 正文/表格单元格的文字最终都承载于段落，程序逐段落处理；
2. **纯文本坐标定位**：读取段落纯文本，通过字符串查找获取所有 `{{标签}}` 的精确起止索引；
3. **坐标映射Run**：通过索引数学比对，精准圈定包含标签的所有Run；
4. **精准替换规则**：首个Run保留格式并填入真实内容，其余关联Run仅删除标签部分文本，非关联内容完整保留。

#### 4.1.2 索引偏移问题解决方案

当**单个段落包含多个标签**时，替换前一个标签会改变段落字符长度，导致后续标签索引失效。
**核心解决策略**：每次仅处理一个标签，处理完成后**实时重新读取段落纯文本**计算索引，杜绝偏移错误。

```python
# 核心秘诀：每次替换前重新获取最新的段落纯文本
def replace_text_in_paragraph(para, old_text, new_text):
    full_text = para.text  # 实时刷新文本，解决索引偏移
    ...
```

#### 4.1.3 算法原理图解

假设段落底层有 6 个 Run:

```
Run 1: "致:"      (长度2, 范围 0-2)
Run 2: "("       (长度1, 范围 2-3)
Run 3: "项目"     (长度2, 范围 3-5)
Run 4: "名称"     (长度2, 范围 5-7)
Run 5: ")"       (长度1, 范围 7-8)
Run 6: "工程"     (长度2, 范围 8-10)

拼起来的纯文本:"致:(项目名称)工程"
```

目标: 将 `(项目名称)` 替换为 `{{招标项目名称}}`

程序执行:
1. 定位: 目标文本在段落中的位置是第 2 个字符到第 8 个字符(长度 6)
2. 指针游走:
   - Run 1 (0-2): 不包含目标文本
   - Run 2 (2-3): 包含目标文本的起始部分 -> 标记为"首个Run"
   - Run 3 (3-5): 完全在目标范围内 -> 后续清空
   - Run 4 (5-7): 完全在目标范围内 -> 后续清空
   - Run 5 (7-8): 包含目标文本的结束部分 -> 后续清空
   - Run 6 (8-10): 超出目标范围 -> **保留!**
3. 执行替换:
   - Run 2 改为 `{{招标项目名称}}`(继承原括号格式)
   - Run 3/4/5 清空为 `""`
   - Run 6 的 "工程" 完美保留

**为什么不会误删?**
- 程序通过计算字符串在原句中的 `Start`(起始)和 `End`(结束)索引来界定范围
- 一旦指针超过目标范围,立即停止清空操作
- Run 6 因为起始位置 8 已经大于目标结束位置 8,所以被安全保留

#### 4.2 表格填充与渲染规则

##### 4.2.1 表格序列化文本原路回写

大模型返回带标签的 `|` 分隔表格文本后：

1. 后端按行用 `|` 分割字符串，得到单元格文本数组；
2. 按**提取时的唯一单元格索引顺序**，原样写回表格对应单元格；
3. 全程不修改Word单元格合并、边框、样式属性，原表格布局100%保留。

##### 4.2.2 docxtpl 统一动态渲染

所有打完标签的文档统一交由 `docxtpl` 引擎渲染：

- **静态表单**：普通 `{{标签}}` 逐一对位填空；
- **动态列表**：识别 `{{tr for item in 列表名称}}` 循环语法，根据JSON数组条数**自动克隆表格行、自动删除多余空白行**；
- 表格字体、字号、对齐、合并单元格、边框样式全部继承模板，无任何格式丢失。

```python
from docxtpl import DocxTemplate

tpl = DocxTemplate('带循环标签的模板.docx')
context = {
    "项目负责人": "周建峰",
    "业绩列表": [
        {"建设单位": "吴中区交投", "工程名称": "尹山湖路网", "质量": "合格"},
        {"建设单位": "高新区住建局", "工程名称": "长江路改造", "质量": "合格"}
    ]
}
tpl.render(context)
tpl.save('最终文档.docx')
```

**docxtpl 的魔法**:
- 数组里有几个项目,程序自动把带有标签的那一行向下复制几遍
- 自动删除多余空白行,或在不够时自动插入新行
- 每一行完美继承原模板的字体、字号、居中等所有表格样式

#### 4.3 遍历文档执行替换
```python
def fill_document(doc, data_dict):
    # 1. 处理正文段落
    for para in doc.paragraphs:
        for tag, value in data_dict.items():
            placeholder = f'{{{{{tag}}}}}'
            if placeholder in para.text:
                replace_text_in_paragraph(para, placeholder, value)

    # 2. 处理表格(俄罗斯套娃遍历)
    for table in doc.tables:
        for row in table.rows:
            for cell in row.cells:
                for para in cell.paragraphs:
                    for tag, value in data_dict.items():
                        placeholder = f'{{{{{tag}}}}}'
                        if placeholder in para.text:
                            replace_text_in_paragraph(para, placeholder, value)

    return doc
```

---

### 第五步: 保存输出

```python
# 保存填充后的文档
doc.save('最终生成的商务标.docx')
```

---

## 四、完整流程图
```
+---------------------------------------------------------------------+
|                        投标文件自动填充系统                           |
+---------------------------------------------------------------------+
|                                                                     |
|  ① 文档预处理                                                       |
|     +- 用户上传 Word / PDF                                          |
|     +- PDF -> Word (pdf2docx)                                        |
|     +- 合并格式相同的相邻 Run(清洗碎片化)                            |
|                          v                                          |
|  ② AI 辅助打标                                                      |
|     +- 提取正文纯文本、遍历所有表格                                  |
|     +- 全表格统一唯一单元格提取 + |分隔符序列化                      |
|     +- 大模型自主识别静态表单/动态列表 差异化打标                    |
|     +- 动态列表自动删减空行、生成docxtpl循环标签                    |
|                          v                                          |
|  ③ 标签提取与企业信息匹配                                           |
|     +- 正则提取所有 {{标签}} 与循环标签                              |
|     +- 大模型输出普通JSON / 列表嵌套JSON                            |
|                          v                                          |
|  ④ 内容填充与表格渲染                                               |
|     +- 跨Run字符索引精准替换，保留原格式                            |
|     +- 表格按分隔符原路回写，不改动合并单元格                        |
|     +- docxtpl统一渲染：静态填空、动态自动克隆表格行                |
|     +- 多标签实时刷新索引，解决偏移问题                              |
|                          v                                          |
|  ⑤ 保存输出                                                         |
|     +- doc.save('最终商务标.docx')                                   |
|                                                                     |
+---------------------------------------------------------------------+
```

---

## 五、关键风险与应对策略
| 风险点 | 影响 | 应对策略 |
|--------|------|----------|
| **PDF转Word后Run碎片化** | 标签被切断到多个Run中,替换失败 | 预处理阶段合并格式相同的相邻Run |
| **大模型省略/截断** | 输出内容不完整,导致废标 | 按段落/单张表格为颗粒度分批发送,不做全文一次性处理 |
| **大模型幻觉篡改原文** | 自行优化句子,改变法律条款含义 | Prompt中强调"严禁改动非空白处",程序端做字符串比对校验 |
| **超长文档超Token** | 几百页标书一次性处理会超限 | 分段处理(按章节或页码切片) |
| **表格嵌套错位** | 复杂表格解析出错 | 递归遍历所有单元格内的段落,统一复用替换逻辑 |
| **跨Run误删** | 清空Run时波及无辜文本 | 使用字符索引映射法精确定位范围,指针超限立即停止 |
| **单段落多标签索引偏移** | 替换后标签位置错乱,误删文本 | 每次替换前重新读取段落纯文本，实时计算索引 |
| **标准Markdown不兼容合并单元格** | 表格解析报错、结构错乱 | 放弃标准Markdown，统一采用\|分隔符伪Markdown序列化 |
| **动态列表逐行固定编号打标** | 数据溢出/残留乱码、模板引擎失效 | 由AI自主识别列表类型，仅生成首行循环泛型标签 |
| **表格行列数不规整解析失败** | 回写单元格错位 | 后端按唯一单元格顺序提取、按索引顺序原路回写 |

---

## 六、给后端开发的代码框架

```python
# ========== 完整可运行的代码框架 ==========
from docx import Document
import re

# --- 核心替换算法 ---
def replace_text_in_paragraph(para, old_text, new_text):
    """跨Run替换,保留格式"""
    full_text = para.text
    if old_text not in full_text:
        return False
    
    start_idx = full_text.find(old_text)
    end_idx = start_idx + len(old_text)
    
    run_start_idx = 0
    matching_runs = []
    
    for run in para.runs:
        run_end_idx = run_start_idx + len(run.text)
        if (run_start_idx <= start_idx < run_end_idx or
            run_start_idx < end_idx <= run_end_idx or
            (start_idx <= run_start_idx and run_end_idx <= end_idx)):
            matching_runs.append({
                'run': run,
                'run_start': run_start_idx,
                'run_end': run_end_idx,
                'text_start': max(start_idx, run_start_idx),
                'text_end': min(end_idx, run_end_idx)
            })
        run_start_idx = run_end_idx
    
    if not matching_runs:
        return False
    
    if len(matching_runs) == 1:
        run_info = matching_runs[0]
        run = run_info['run']
        local_start = run_info['text_start'] - run_info['run_start']
        local_end = run_info['text_end'] - run_info['run_start']
        run.text = run.text[:local_start] + new_text + run.text[local_end:]
        return True
    
    # 跨Run处理: 保留首个Run格式
    first = matching_runs[0]
    first_run = first['run']
    first_local_start = first['text_start'] - first['run_start']
    first_run.text = first_run.text[:first_local_start] + new_text
    
    for i, run_info in enumerate(matching_runs):
        if i == 0:
            continue
        run = run_info['run']
        local_start = run_info['text_start'] - run_info['run_start']
        local_end = run_info['text_end'] - run_info['run_start']
        if local_end < len(run.text):
            run.text = run.text[local_end:]
        else:
            run.text = ""
    return True

# --- 主流程 ---
def process_bid_document(template_path, output_path, data_dict):
    doc = Document(template_path)
    
    # 1. 处理正文
    for para in doc.paragraphs:
        for tag, value in data_dict.items():
            placeholder = f'{{{{{tag}}}}}'
            if placeholder in para.text:
                replace_text_in_paragraph(para, placeholder, value)
    
    # 2. 处理表格(俄罗斯套娃遍历)
    for table in doc.tables:
        for row in table.rows:
            for cell in row.cells:
                for para in cell.paragraphs:
                    for tag, value in data_dict.items():
                        placeholder = f'{{{{{tag}}}}}'
                        if placeholder in para.text:
                            replace_text_in_paragraph(para, placeholder, value)
    
    doc.save(output_path)
    print(f"文档已保存至: {output_path}")

# --- 使用示例 ---
if __name__ == '__main__':
    data = {
        "招标人名称": "苏州工业园区城市重建有限公司",
        "招标项目名称": "苏州工业园区星塘街北段市政配套提升工程(标段二)",
        "投标人名称": "江苏华宇市政建设集团有限公司",
        "法定代表人姓名": "陈铭宇",
        "单位地址": "江苏省苏州市工业园区苏州大道东288号领汇广场15楼",
        "邮政编码": "215028",
        "固定电话": "0512-62XXXX77",
        "传真": "0512-62XXXX78",
        "年": "2026",
        "月": "05",
        "日": "09",
        "注册资金": "10000万元人民币",
        "成立时间": "2006年08月22日",
        "注册地址": "江苏省苏州市工业园区苏州大道东288号领汇广场15楼",
        "员工总数": "286人",
        "联系人姓名": "林晓棠",
        "联系人电话": "135XXXX7766",
        "法定代表人电话": "136XXXX9988",
        "资质类型": "市政公用工程施工总承包",
        "资质等级": "壹级",
        "开户银行": "中国工商银行股份有限公司苏州工业园区支行",
        "银行账号": "1102020309XXXX08888",
        "近三年营业额": "营业收入年均3.2亿元",
        "关联企业情况": "无"
    }
    
    process_bid_document('模板.docx', '最终商务标.docx', data)
```

---

## 七、总结
这套技术路线的核心设计哲学是 **"关注点分离"**:

| 角色 | 职责 | 工具 |
|------|------|------|
| **大模型** | 语义理解(识别空白处、匹配企业信息) | Prompt A / Prompt B |
| **Python后端** | 格式处理(解析XML、精准替换、保存文档) | python-docx / docxtpl |

**优势**:
1. **通用性强**: 适用于任何类型的投标文件模板,不依赖固定字段
2. **格式100%保留**: 通过 Run 级操作,字体、字号、加粗、下划线、表格样式全部继承
3. **可扩展**: 支持静态表单 + 动态列表两种表格模式
4. **可维护**: AI只输出纯文本和JSON,不碰格式代码,降低出错概率